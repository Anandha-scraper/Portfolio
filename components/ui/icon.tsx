import {
  Compass,
  Network,
  Boxes,
  Route,
  FlaskConical,
  Newspaper,
  Send,
  LayoutTemplate,
  Server,
  Database,
  Cloud,
  Code2,
  Award,
  Medal,
  GraduationCap,
  Rocket,
  School,
  Layers,
  Trophy,
  TerminalSquare,
  Wand2,
  ShieldCheck,
  Github,
  Linkedin,
  Mail,
  Phone,
  ArrowUpRight,
  ArrowRight,
  ArrowDown,
  ExternalLink,
  MapPin,
  Sparkles,
  Plus,
  Minus,
  X,
  Download,
  Copy,
  Check,
  CornerDownLeft,
  Circle,
  Dot,
  ChevronRight,
  Briefcase,
  Info,
  Sun,
  Moon,
  type LucideIcon,
} from "lucide-react";

const ICONS = {
  Compass,
  Network,
  Boxes,
  Route,
  FlaskConical,
  Newspaper,
  Send,
  LayoutTemplate,
  Server,
  Database,
  Cloud,
  Code2,
  Award,
  Medal,
  GraduationCap,
  Rocket,
  School,
  Layers,
  Trophy,
  TerminalSquare,
  Wand2,
  ShieldCheck,
  Github,
  Linkedin,
  Mail,
  Phone,
  ArrowUpRight,
  ArrowRight,
  ArrowDown,
  ExternalLink,
  MapPin,
  Sparkles,
  Plus,
  Minus,
  X,
  Download,
  Copy,
  Check,
  CornerDownLeft,
  Circle,
  Dot,
  ChevronRight,
  Briefcase,
  Info,
  Sun,
  Moon,
} satisfies Record<string, LucideIcon>;

export type IconName = keyof typeof ICONS;

interface IconProps extends React.SVGProps<SVGSVGElement> {
  name: string;
  size?: number;
}

/** Renders a curated lucide icon by name. Falls back to a dot if unknown. */
export function Icon({ name, size = 18, ...props }: IconProps) {
  const Cmp = (ICONS as Record<string, LucideIcon>)[name] ?? Dot;
  return <Cmp width={size} height={size} {...props} />;
}
